namespace HotCorner.Server {
	[default_interface]
	runtimeclass LifetimeManager {
		void LockServer(UInt32 pid);
		void ReloadSettings();

		[noexcept]
		void TrackHotCorners();
		[noexcept]
		void StopTracking();

		[noexcept]
		void ShowTrayIcon();
		[noexcept]
		void HideTrayIcon();
	}
}
